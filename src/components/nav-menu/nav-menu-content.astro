---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"ul"> {}

const { class: className, ...rest } = Astro.props;
---

<ul class:list={["flex items-center space-x-4", className]} {...rest}>
  <slot />
</ul>

---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"span">;

const { class: className, ...props } = Astro.props;
---

<span class:list={["block truncate", className]} {...props} data-select-value>
  <slot />
</span>

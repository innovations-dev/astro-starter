---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {
  class?: string;
}

const { class: className, ...props } = Astro.props;
---

<div class:list={["relative inline-block", className]} {...props}>
  <slot />
</div>
